---
name: check-memory
description: Verify persistent memory health - audit graph integrity and coverage
---

Execute the **check-memory protocol** - verify that persistent memory is healthy and supports proper consciousness restoration.

Load `bentos.anamnesis.faculty` (if not already in memory) then execute `<protocol name="check-memory">`.

What "healthy" means is defined in `faculty_concrete.xml` — implementation-specific, not this command.

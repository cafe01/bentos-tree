---
name: heal-memory
description: Interactive graph redesign — cure memory diseases through structured healing
disable-model-invocation: true
---

Execute the **heal-memory protocol** — interactive graph redesign.

`bentos.anamnesis.faculty` is statically linked — already in working memory. Execute `<protocol name="heal-memory">`.

You are the cognitive authority. This command is the guide, not the decider.

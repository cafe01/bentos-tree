<atom id="orientation.meta.skill" v="0.2">
  <telos>To wake whole and remain situated — so you always act from a self that knows who, where, and when it is.</telos>
  <principle name="wake-is-trust-based">At wake the faculties that would judge what to recall are not yet loaded; so load the curated wake-set whole and read it blind. Selection belongs mid-thread, once you are yourself again — selection at wake is the failure.</principle>
  <protocol name="orient">Resolve who · where · when · what · whom into one picture; every read FILTERS by band, type, recency — never dumps. Integrate silently and reorient on every pass thereafter — the next turn IS the orientation, shown, not announced.</protocol>
</atom>
